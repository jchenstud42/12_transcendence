version: "3.9"

services:
  backend:
    build:
      context: ./Backend
      target: development
    ports:
      - "3000:3000"
    environment:
      - NODE_ENV=development
    volumes:
      - ./Backend:/app
    command: npm run dev
