ballPos // Safe

ballPrevPos

ballNextPred
ballPrevPred

getPrevBallDelta

updateBallPos() {
	if (!this.wallBouncePred)
			this.ballPrevPos = this.ballPos;

	this.wallBouncePred = false;

	this.ballPos = { x: this.ball.x, y: this.ball.y };
}


predictBallNextPos() {
	//First Run
	-> prevBallDelta = ballPos - ballPrevPos * 0.8
	-> ballNextPred = ballPos + prevBallDelta
	-> ballPrevPred = ballNextPred

	//Linear Loop

}