server {
    listen 80;
    server_name localhost;

    return 301 https://$host$request_uri;
}

server {
    listen 0.0.0.0:8443 ssl;
	listen [::]:8443 ssl;
    server_name _;

    ssl_certificate     /etc/nginx/certs/fullchain.pem;
    ssl_certificate_key /etc/nginx/certs/privkey.pem;

    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_ciphers HIGH:!aNULL:!MD5;

    root /usr/share/nginx/html;
    index index.html;

    location /js/ {
        try_files $uri =404;
    }

    # REGISTER
    location /register {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/register;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }

    # LOGIN
    location /login {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/login;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }

    # LOGOUT
    location /logout {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/logout;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }

    # USER
    location /user/ {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/user/;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }

	location /enable-2fa {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/enable-2fa;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }
	location /disable-2fa {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/disable-2fa;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }
	location /verify-2fa {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/verify-2fa;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }
	location /enable-totp {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/enable-totp;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }
	location /verify-totp {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/verify-totp;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }

	location /oauth/42 {
		proxy_pass http://backend:3000/oauth/42;
		proxy_set_header Host $host;
		proxy_set_header X-Forwarded-Proto $scheme;
	}

	location /oauth/42/callback {
		proxy_pass http://backend:3000/oauth/42/callback;
		proxy_set_header Host $host;
		proxy_set_header X-Forwarded-Proto $scheme;
	}

	location /friend/request/accept {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/friend/request/accept;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }

	# FRIENDS

    location /friend/request/reject {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/friend/request/reject;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }

    location /friend/request/received {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/friend/request/received;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }

	location /friend/request {
        if ($request_method = OPTIONS) {
            add_header Access-Control-Allow-Origin "https://localhost:8443" always;
            add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
            add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
            add_header Access-Control-Allow-Credentials "true" always;
            add_header Content-Length 0;
            return 204;
        }
        proxy_pass http://backend:3000/friend/request;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_http_version 1.1;
        proxy_set_header Connection "";
        add_header Access-Control-Allow-Origin "https://localhost:8443" always;
        add_header Access-Control-Allow-Credentials "true" always;
    }


	location /friend {
		if ($request_method = OPTIONS) {
			add_header Access-Control-Allow-Origin "https://localhost:8443" always;
			add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
			add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
			add_header Access-Control-Allow-Credentials "true" always;
			add_header Content-Length 0;
			return 204;
		}
		proxy_pass http://backend:3000/friend;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header X-Forwarded-Proto $scheme;
		proxy_http_version 1.1;
		proxy_set_header Connection "";
		add_header Access-Control-Allow-Origin "https://localhost:8443" always;
		add_header Access-Control-Allow-Credentials "true" always;
    }

	location /match {
		if ($request_method = OPTIONS) {
				add_header Access-Control-Allow-Origin "https://localhost:8443" always;
				add_header Access-Control-Allow-Methods "GET, POST, OPTIONS, PUT, DELETE" always;
				add_header Access-Control-Allow-Headers "Content-Type, Authorization, X-Requested-With" always;
				add_header Access-Control-Allow-Credentials "true" always;
				add_header Content-Length 0;
				return 204;
		}
		proxy_pass http://backend:3000/match;
		proxy_set_header Host $host;
		proxy_set_header X-Real-IP $remote_addr;
		proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
		proxy_set_header X-Forwarded-Proto $scheme;
		proxy_http_version 1.1;
		proxy_set_header Connection "";
		add_header Access-Control-Allow-Origin "https://localhost:8443" always;
		add_header Access-Control-Allow-Credentials "true" always;
	}

    # Fallback SPA
    location / {
		try_files $uri /index.html;
    }
}
